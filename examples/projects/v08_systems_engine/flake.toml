[package]
name = "v08_systems_engine"
version = "0.8.0"
entry = "main.flk"
authors = ["Flake Team"]
description = "Flake v0.8 flagship showcase: generics, systems stdlib, channels, nursery concurrency, and escape analysis."

[dependencies]
